import styles from './SupportPage.module.css';

const SupportPage = () => {
    return (
        <section className={styles.support_section}>
            <h1 className={styles.support_title}>Support</h1>
        </section>
    )
}

export default SupportPage;